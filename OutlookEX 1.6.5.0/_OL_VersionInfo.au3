#include <OutlookEX.au3>

;------------------------------------------------------------------------------------------------------------------------------------------------
; Example 1 - Display the version info for this UDF
;------------------------------------------------------------------------------------------------------------------------------------------------
Global $sVersionInfo = _OL_VersionInfo()
MsgBox($MB_ICONINFORMATION, "_OL_VersionInfo", $sVersionInfo)